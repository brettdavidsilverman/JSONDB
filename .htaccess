RewriteEngine on

<FilesMatch "\.(html|htm|js|css)$">
  FileETag None
  <IfModule mod_headers.c>
    Header unset ETag
    Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "Wed, 12 Jan 1980 05:00:00 GMT"
  </IfModule>
</FilesMatch>


# redirect https://bee.fish -> index.php
RewriteRule ^$ /index.php [END]
# redirect all root files to same and exit
RewriteRule ^((.*)\.(php|css|html|js))$ /$1 [END]
# redirect client to same and exit
RewriteRule ^client/(.*)$ client/$1 [END]
# redirect server to same and exit
RewriteRule ^server/(.*)$ server/$1 [END]
# Redirect all else to JSON.php
RewriteRule ^(.*)$ server/JSON.php [END]
